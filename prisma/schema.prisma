// ... (keep existing models)

model SiteSettings {
  id              String @id @default(cuid())
  siteName        String
  siteUrl         String
  defaultLanguage String
  metaDescription String?
  metaKeywords    String?
}

// ... (keep other models)

