generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

// De rest van je modellen blijven hetzelfde, maar verwijder alle @@schema attributen

